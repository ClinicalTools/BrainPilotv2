Advanced Dissolve (LightweightPipeline).unitypackage contains shaders and example scenes for Lightweight Render Pipeline (package v3.0.0).

Note, Lightweight Render Pipeline is still in preview mode.
Remove original Advanced Dissolve folder after or before importing this package.

